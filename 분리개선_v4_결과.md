# 팔레트 혼입 개선 v4 — 2026-09-11

프로그램의 기본 선택을 **팔레트 혼입 개선 (v4)**로 연결했습니다. 프로그램을 다시 실행하면 적용됩니다. 기존 v2·v3는 모델 선택에서 계속 사용할 수 있습니다. 이미 저장한 라벨 파일은 열기만 하면 자동으로 다시 라벨링하지 않으므로 재처리하려면 프로그램의 자동 라벨링 기능을 사용해야 합니다.

## 변경 사항

기존 분리 로직은 부자재로 둘러싸인 영역을 `binary_fill_holes`로 전부 채웠습니다. 이 과정에서 분류기가 팔레트로 판단한 실제 관측점까지 부자재로 바뀌었습니다. 이후 영역 분리에서 이 넓은 팔레트 영역이 부자재와 합쳐질 수 있었습니다.

v4는 관측된 점의 부자재 확률이 0.5 미만이면 내부 구멍 채우기로 덮지 않습니다. 관측점이 없는 작은 구멍은 기존 거리 제한 안에서 계속 채웁니다. 물체 경계 모델과 최소 영역 150셀은 유지했습니다.

v4는 **128개로 학습한 v3 분류기 + 개선된 후처리**입니다. 이번 작업에서 200개 전체를 새로 학습한 것은 아닙니다. v4 모델 파일에 후처리 설정을 저장했고, 이전 모델은 기존 후처리를 그대로 사용하도록 호환성을 유지했습니다.

## 선택과 검증

검증 20개에서 v2·v3, 분리 기준 0.03·0.06, 구멍 채우기 확률 제한 0·0.2·0.5의 12가지 조합을 비교했습니다. 기존 v2 기본 설정보다 팔레트 IoU·부자재 IoU·물체 F1이 모두 낮지 않은 개선 후보 중 부자재 IoU가 가장 높은 조합을 선택했습니다. 선택 결과는 v3, 분리 기준 0.03, 채우기 확률 0.5입니다.

| 검증 20개 | v2 | v3 | v4 |
|---|---:|---:|---:|
| 부자재 IoU | 87.68% | 87.94% | 88.81% |
| 팔레트 IoU | 99.27% | 99.24% | 99.51% |
| 물체 F1 | 89.44% | 89.19% | 89.85% |

검증에서 선택을 마친 뒤 기존 비교 파일 22개에서 재확인했습니다.

| 비교 파일 / 부자재 IoU | v2 | v3 | v4 |
|---|---:|---:|---:|
| 128~133 | 86.67% | 85.83% | 87.80% |
| 166~171 | 89.49% | 88.35% | 90.01% |
| 295~304 | 94.01% | 94.30% | 94.83% |

물체 F1은 각각 v2→v4 기준 84.84%→84.23%, 87.04%→87.06%, 92.79%→93.37%입니다. 첫 묶음의 물체 매칭 지표는 소폭 하락하므로 모든 지표·장면이 개선된 것은 아닙니다.

이 22개 파일은 앞선 원인 분석에서도 살펴본 자료이므로 이번 점수는 회귀 확인 결과이며, 한 번도 보지 않은 새 현장 데이터에 대한 성능 증명은 아닙니다.

## 실패 사례 재확인

- 171.ply: 부자재 IoU v3 79.34%→v4 87.45%. 물체 7에 붙은 정답 팔레트 점은 35,146개→66개.
- 131.ply: 부자재 IoU 75.48%→80.57%.
- 129.ply: 부자재 IoU 84.83%→88.24%.

원본 171.ply를 실제 라벨링하고 PLY로 저장한 뒤 다시 읽어 590,199개 점과 원본의 모든 필드가 그대로 유지되는 것을 확인했습니다. 결과 예시는 `results/segmentation_v4/example/171_labeled.ply`에 있습니다. 자동 초안이며 사람이 검수한 정답 파일은 아닙니다.

관측된 팔레트 구멍 보존, 작은 미관측 구멍 연결, 전체 팔레트 입력, 모델별 설정 전달의 4개 자동 테스트가 통과했습니다.

## 결과와 재현

- `results/segmentation_v4/model_v4.joblib`: 개선판 모델과 후처리 설정.
- `results/segmentation_v4/validation.json`: 12가지 조합의 검증 결과.
- `results/segmentation_v4/regression.json`: 22개 파일의 모델별 결과.
- `improve_segmentation.py`: 검증 선택 및 비교 재현 스크립트.
- `test_segmentation.py`: 핵심 동작 테스트.

재현: `.venv/bin/python improve_segmentation.py`

테스트: `.venv/bin/python -m unittest test_segmentation.py`

CLI는 기존 기본 모델 동작을 유지하며, v4는 `--model results/segmentation_v4/model_v4.joblib`로 지정할 수 있습니다. GUI는 `model_config.json`의 첫 항목인 v4를 기본으로 선택합니다.
