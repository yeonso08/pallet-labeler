# 추가 데이터 재학습 결과 — v2

2026-09-09에 제공한 다운로드 폴더의 데이터를 사용해 재학습했습니다. 기존 프로그램의 기본 모델을 v2로 업데이트했고, 화면 왼쪽에서 **추가 학습 모델 (v2)** 또는 **이전 모델 (v1)**을 선택할 수 있습니다. 이미 실행 중인 프로그램은 닫고 다시 실행해야 업데이트를 불러옵니다. 열려 있는 수정 내용은 먼저 저장하세요.

## 실제 사용한 데이터

- 추가 원본: 109개, 134~242.
- 추가 라벨링 후 파일: 108개. 226번 정답 파일 없음.
- `scalar_instance_label`이 있는 파일: 48개. 이 중 36개 추가 학습, 6개 분리 기준 조정, 6개 최종 비교.
- `scalar_Original_cloud_index`만 있는 파일: 60개. 이 필드가 정답 라벨로 사용 가능한지 확인 전이므로 **학습과 평가에서 제외**했습니다.
- 기존 학습 19개 + 추가 학습 36개 = **총 55개 장면으로 학습**.

정답 `instance_label`과 분할 번호가 함께 있는 추가 파일 48개에서는 모두 `instance_label = Original_cloud_index + 1` 관계가 정확히 일치했습니다. 그러나 명시적인 라벨이 없는 60개에도 동일한 의미가 적용되는지는 사용자 확인이 필요합니다. 원본이나 정답 파일을 임의로 변환하지 않았습니다.

## 같은 파일로 기존 모델과 비교

| 평가 데이터 | 항목 | v1 | v2 |
|---|---|---:|---:|
| 추가 파일 166~171, 6개 | 부자재 평균 IoU | 88.51% | **89.49%** |
| 추가 파일 166~171, 6개 | 팔레트 IoU | 97.803% | 97.802% |
| 추가 파일 166~171, 6개 | 물체 F1, IoU 0.5 기준 | 87.04% | 87.04% |
| 기존 파일 128~133, 6개 | 부자재 평균 IoU | 86.62% | **86.97%** |
| 기존 파일 128~133, 6개 | 팔레트 IoU | 97.109% | 97.119% |
| 기존 파일 128~133, 6개 | 물체 F1, IoU 0.5 기준 | 84.06% | 84.06% |

**부자재 영역은 소폭 개선됐고, 물체 분리 오류가 크게 줄어든 것은 아닙니다.** 새로운 데이터의 부자재 IoU 개선은 약 0.98%포인트입니다. 이것을 모든 점의 정답률이나 오류 없는 파일의 비율로 해석하면 안 됩니다. 겹침과 가림으로 인해 하나의 물체가 나뉘는 오류는 남아 있습니다.

새 데이터의 v1 성능 88.51%와 이전 대화의 86.62%는 평가 파일이 다릅니다. 개선 폭은 반드시 표의 **같은 행**에서 비교해야 합니다.

## 검증 방법

모델 학습과 최종 평가 파일을 분리했습니다. 기존 124~127 및 추가 160~165 총 10개 조정용 파일에서 분리 기준 0.03, 0.06, 0.1, 0.15, 0.2, 0.25를 비교해 0.06을 선택했습니다. 최종 비교 파일 12개는 학습과 매개변수 선택에 사용하지 않았습니다.

기존 알고리즘과 같은 격자 크기 4, 최소 영역 150셀 및 같은 분류기 구조를 사용했습니다. 추가 데이터와 분리 기준 조정의 효과를 함께 비교한 결과입니다. IoU는 정답·예측 물체를 최대 겹침으로 대응한 후 계산했습니다. 정답에서 삭제된 점 등 좌표가 정확히 일치하지 않는 점은 점수에서 제외합니다.

같은 스캐너·촬영 환경에서 수집한 연속 파일들이므로 서로 상관관계가 있을 수 있습니다. 이 12개 파일만으로 다른 현장·물체의 성능이나 통계적으로 확실한 개선을 보장하지 않습니다.

## 확인 대기 파일

Original cloud index만 있는 60개: **134, 172~199, 211~225, 227~242**.

해당 파일들이 물체별 분할 완료 상태이며 **0이 팔레트, 나머지 번호가 각 부자재**라는 확인을 받으면, 원본 파일을 보존하면서 학습 시에만 1을 더한 라벨로 해석해 추가 재학습할 수 있습니다. 226번은 정답 파일을 별도로 받아야 합니다.

## 결과 파일

- `model.joblib`: v2 기본 모델.
- `models/model_v1.joblib`: 이전 모델 보관본.
- `model_config.json`: 프로그램 모델 선택 및 기본 분리 기준.
- `examples_v2/166_labeled.ply`, `examples_v2/128_labeled.ply`: v2 자동 초안. 수동 수정하지 않음.
- `examples/`: 첫 버전으로 생성한 예시. 그대로 보존함.
- `evaluation_v2.json`: 파일별 이전/새 모델 평가, 매개변수 비교, 학습 명세.
- `training_manifest_v2.json`: 학습·조정·평가 파일 및 라벨 출처.
- `retrain.py`: 명세 파일 기반의 재학습 스크립트. 원본 파일을 쓰거나 고치지 않음.

재현 명령은 프로그램 폴더에서 다음과 같습니다. 결과 모델은 별도 경로를 지정해 기존 모델을 보존하세요.

```sh
.venv/bin/python retrain.py training_manifest_v2.json --cache /임시/학습캐시 --output /새모델/model.joblib
```

이 명령은 학습을 재현하며 분리 기준 자동 탐색을 수행하지 않습니다. 현재 배포 모델의 선택 기준은 0.06이고, 모든 비교 결과는 evaluation_v2.json에 기록되어 있습니다.
