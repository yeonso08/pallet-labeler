# Changelog

이 프로젝트의 주요 변경사항을 버전별로 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따르고,
버전 번호는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

여러 차례에 걸쳐 들어간 모델 개선과 세밀 수정 기능을 한꺼번에 기록합니다.
전체 사용법은 `사용법.md`로 새로 정리했습니다.

### Added
- **가림·조각 연결 개선 (v5) 모델이 기본값입니다.** 가려져 여러 조각으로 보이는 하나의 부자재를 다시 이어 붙입니다. 높이·평면 방향·색상·거리·크기 등 29개 특성으로 조각 쌍을 비교하며, 검증 20개 평균 물체 F1이 89.85%에서 94.45%로, 부자재 IoU가 88.81%에서 89.66%로 올랐습니다. 상세: `조각연결_v5_결과.md`
- **팔레트 혼입 개선 (v4) 모델.** 부자재에 둘러싸인 관측 팔레트 점까지 부자재로 채우던 후처리를 고쳤습니다. 검증 20개 팔레트 IoU 99.27% → 99.51%. 상세: `분리개선_v4_결과.md`
- **새 데이터 학습 모델 (v3).** 기존 장면에서 성능이 내려가 기본으로 쓰지 않습니다. 상세: `학습결과_v3.md`, `성능하락_원인분석.md`
- **부재 고정.** 부재를 왼쪽 클릭하면 그 라벨 전체가 선택되고, 이후 드래그 선택이 그 라벨의 점으로 제한됩니다. 화면상 같은 자리에 겹친 팔레트나 다른 부재는 들어오지 않습니다. Shift·Alt로 더하고 빼도 범위를 넘지 않습니다.
- **선택 부재만 보기 / 전체 함께 보기.** 고정한 부재만 남기고 나머지를 숨깁니다.
- **보는 방향 버튼 위 · 앞 · 옆 · 3D.** 각각 XY, XZ, YZ, 회전 가능한 입체 화면입니다. 겹쳐 보이는 부재를 다른 각도에서 확인할 때 씁니다.
- **2D 버튼.** 3D에서 빠져나와 마지막에 보던 평면으로 돌아갑니다. 평면을 다시 고를 필요가 없습니다.
- **평면마다 확대 상태를 기억합니다.** 앞에서 확대해 보다가 위로 갔다 돌아오면 보던 배율 그대로입니다. **화면 맞춤**은 지금 보는 평면만 처음 배율로 되돌립니다.
- **키보드 단축키.** `1` `2` `3`이 위·앞·옆, `Tab`이 2D와 3D 전환입니다. 라벨 번호를 입력하는 중에는 동작하지 않습니다.
- 선택한 점 중 일부가 가려져 빠지면 몇 개가 빠졌는지 상태줄에 알려 줍니다. 전부 가려져 있으면 아무것도 바꾸지 않고 이유를 알려 줍니다. 이전에는 조용히 빠졌습니다.
- **두께 확대.** 앞·옆 화면에서 얇은 부재를 세로로 늘려 그립니다. 끄면 실제 좌표 비율입니다.
- **구간 좁히기.** 높이 Z, 깊이 Y, 가로 X 중 한 축을 골라 시작·끝 슬라이더로 표시 범위를 줄입니다. 옆에 원본 좌표 단위의 실제 범위를 보여 줍니다. 숨긴 구간의 점은 선택할 수도, 라벨을 바꿀 수도 없습니다.
- 부재 전체를 클릭해 선택했을 때는 흰색으로 덮지 않고 원본 색을 비쳐 표시합니다. 영역 드래그로 고른 점과 구분됩니다.
- 명령줄 `label.py`에 `--model` 옵션. v5를 쓰려면 `--model results/region_link_v5/model_v5.joblib`을 지정합니다.
- 선택·분리·연결 자동 테스트 25개(`test_app_selection.py`, `test_segmentation.py`, `test_region_affinity.py`)와 실제 Tk 화면 검사(`test_app_view_smoke.py`). 화면 검사는 단축키와 평면별 배율 기억까지 확인합니다.
- **`사용법.md`를 전체 설명서로 새로 썼습니다.** 실행, 화면 구성, 모델 선택, 마우스 조작, 세밀 수정, 저장, 일괄 처리, 명령줄, 성능과 한계, 문제 해결, 파일 구성을 한 문서에 담았습니다.

### Changed
- **보는 방법을 바꿔도 선택이 사라지지 않습니다.** 방향 전환, 따로 보기, 구간 슬라이더가 모두 고른 점을 유지합니다. 옆에서 고르고 위에서 확인한 뒤 그대로 적용할 수 있습니다. 선택을 지우는 것은 Esc, 선택 해제, 라벨 적용뿐입니다. 숨겨진 점에 라벨이 붙지 않도록 막는 일은 적용 시점의 교집합이 이미 하고 있어, 선택을 지우는 것은 중복이었습니다.
- **방향 버튼이 따로 보기를 켜지 않습니다.** 이전에는 부재를 고정한 채 방향을 바꾸면 무조건 그 부재만 남아, 전체를 위에서 보려면 버튼을 한 번 더 눌러야 했습니다. 이제 방향은 보는 각도만 바꾸고 따로 보기는 전용 버튼이 맡습니다.
- Esc와 **선택 해제**는 위에서 본 전체 화면으로 돌아갑니다. 이전에는 옆에서 보다가 해제하면 방향 표시와 실제 화면이 어긋났습니다.
- 기본 모델이 추가 학습 모델 (v2)에서 가림·조각 연결 개선 (v5)로, 기본 분리 기준이 0.06에서 0.03으로 바뀌었습니다. v2와 v1은 모델 목록에서 계속 고를 수 있습니다.
- `2D / 3D` 전환 버튼이 방향 버튼 줄의 **2D · 위 · 앞 · 옆 · 3D**로 바뀌었습니다.
- 자동 라벨링이 모델 파일에 저장된 후처리 설정을 따릅니다. 설정이 없는 이전 모델은 기존 동작을 그대로 유지합니다.
- 라벨을 바꾼 점의 `scalar_review_needed`를 0으로 내립니다. 사람이 확인한 점으로 봅니다.

### Fixed
- **확대하면 화면에 보이는 점의 상당수가 클릭도 드래그도 되지 않던 문제.** 네 칸 확대한 화면 기준으로 3D는 그려진 점의 32%, 위에서 보기는 22%가 마우스에 반응하지 않았습니다. matplotlib이 3D 축 영역을 정사각형으로 잡고 같은 비율 보기의 축 영역을 줄이는데, 점군은 그 바깥까지 그려졌기 때문입니다. 축 영역이 화면 전체를 덮도록 바꿨고, 창 크기를 바꾸면 다시 맞춥니다. 화면에 보이는 크기와 위치는 그대로입니다.
- **3D에서 클릭이 눈에 보이는 부재 대신 뒤에 가려진 것을 고르던 문제.** 다섯 번에 한 번 꼴이었습니다. 이제 커서 아래에서 보는 사람에게 가장 가까운 점을 고릅니다. 위에서 보기에서는 가장 높은 점입니다.
- **가려진 하나의 부자재를 여러 물체로 나누던 과분할.** 401번은 정답 9개에 자동 15개였으나 v5에서 9개가 됐습니다.
- **부자재에 둘러싸인 팔레트 점이 부자재로 바뀌던 문제.** 171번에서 물체 7에 붙은 정답 팔레트 점이 35,146개에서 66개로 줄었습니다.
- 부재를 다른 번호로 모두 바꿔 라벨이 사라지면 고정과 보는 방향이 함께 풀립니다. 저장하며 번호를 정리할 때는 고정 번호도 새 번호를 따라갑니다.

위 수치는 이미 여러 번 살펴본 파일들에 대한 회귀 확인입니다. 새 현장 데이터의 성능 증명이 아닙니다.

## [2.2.0] - 2026-09-10

### Added
- **파일 목록에서 파일을 뺄 수 있습니다.** 목록에서 고르고 버튼을 누르거나 Delete(맥에서는 Backspace) 키를 누르면 됩니다. 여러 개를 한 번에 고를 수도 있고, 목록에서만 빠지며 디스크의 파일은 그대로입니다. 지금 열어 놓은 파일을 빼면 화면에서도 닫히며, 저장하지 않은 수정이 있으면 먼저 물어봅니다.
- **수정 결과를 저장할 때 이름과 위치를 지정합니다.** 이전에는 `원래이름_labeled.ply`로 자동 저장됐습니다. 이미 있는 파일은 덮어쓸지 물어보고, 불러온 원본 PLY는 덮어쓸 수 없습니다.
- **3D에서도 점을 선택하고 수정할 수 있습니다.** 이전에는 2D에서만 편집할 수 있었습니다.
- Shift + 드래그로 선택에 추가, Alt + 드래그로 선택에서 빼기.
- 2D에서 오른쪽·휠 드래그로 화면 이동. 이전에는 이동 수단이 없었습니다.
- Esc, 또는 이미 선택된 물체를 다시 클릭하면 선택 해제.
- `theme.py`: 화면 색상·글꼴·컨트롤 스타일을 한곳에서 관리합니다.

### Changed
- 화면 전체를 점군 캔버스와 같은 어두운 색으로 통일했습니다. 강조색은 스캔 속 팔레트 목재의 주황색이며, 저장 버튼과 켜져 있는 표시 버튼에만 사용합니다. **위젯 배치는 그대로입니다.**
- 팔레트·번호 표시가 체크박스에서 켜짐/꺼짐 버튼으로 바뀌었습니다. clam 테마의 체크 표시가 ✕ 모양이라 "해제"처럼 읽혔습니다.
- 마우스 조작을 2D와 3D에서 같게 맞췄습니다. 왼쪽 드래그는 영역 선택, 왼쪽 클릭은 물체 선택, 오른쪽 드래그는 3D 회전 / 2D 이동입니다. 3D 회전이 왼쪽에서 오른쪽 드래그로, 물체 선택이 오른쪽에서 왼쪽 클릭으로 바뀌었습니다.
- 화면 아래 툴바에서 이동·확대 버튼을 뺐습니다. 이제 마우스로 직접 합니다.
- 3D 회전·확대와 2D 이동이 훨씬 부드러워졌습니다. 화면이 움직이는 동안에는 점을 2만 개만 굵게 그리고, 멈추면 전체 밀도로 다시 그립니다 (58만 점 기준 프레임당 약 404ms → 50ms).
- 색상·밀도·표시 변경과 2D/3D 전환이 성긴 화면을 먼저 그린 뒤 채우는 방식으로 바뀌어 즉시 반응합니다 (약 442ms → 78ms).

### Fixed
- **점군 화면의 한글이 네모(두부)로 깨지던 문제.** matplotlib은 Tk와 폰트를 따로 쓰는데 기본 폰트에 한글 글리프가 없었습니다. 화면 폰트도 한글 글꼴로 맞췄습니다.
- **확대하면 점군의 좌우가 잘리던 문제.** matplotlib이 3D 영역을 정사각형으로 강제해서 가로로 긴 화면에서는 좌우 13%가 그리기 영역 밖이었습니다. 이제 화면 전체에 그립니다.
- 파일 이름·점 개수 표시를 화면 왼쪽 위 모서리로 옮겼습니다. 3D 그리기 영역이 안쪽으로 들어가 있어 어중간한 위치에 찍혔습니다.
- 물체 번호 위치 계산과 표시 점 추출을 캐시해, 다시 그릴 때마다 전체 점을 반복 스캔하지 않습니다.

## [2.1.0] - 2026-09-10

레포 초기 커밋 시점 기준. `사용법.md`에 기록된 "v2.1 화면 개선" 이후 상태를 반영합니다.
이전 v1~v2 개발 이력은 `검증기록.md`, `추가학습_결과.md`를 참고하세요.

### Added
- 팔레트/부자재 라벨링 앱 (`app.py`, `engine.py`, `viewer.py`)
- ExtraTrees 기반 분류 모델 및 학습/재학습 스크립트 (`train.py`, `retrain.py`)
- v1/v2 모델 파일 및 평가 결과 (`model.joblib`, `models/model_v1.joblib`, `evaluation.json`, `evaluation_v2.json`)
- 사용법 및 검증 기록 문서

[Unreleased]: https://github.com/yeonso08/pallet-labeler/compare/v2.2.0...HEAD
[2.2.0]: https://github.com/yeonso08/pallet-labeler/compare/v2.1.0...v2.2.0
[2.1.0]: https://github.com/yeonso08/pallet-labeler/releases/tag/v2.1.0
