# 첫 버전(v1) 데이터와 검증 기록

현재 v2 기록은 [추가학습 결과](추가학습_결과.md)를 확인하세요. 이 문서는 최초 버전의 기록을 보존한 것입니다.

## 데이터 분할

- 원본: 바탕화면 `2` 폴더, 103~133 총 31개.
- 정답: 바탕화면 `1` 폴더.
- 학습: 103~123 중 111, 113 제외, 총 19개 장면.
- 조정용 검증: 124~127, 총 4개.
- 최종 평가: 128~133, 총 6개. 모델 학습과 분리 기준 선택에 사용하지 않음.
- 111: 정답 `scalar_instance_label` 없음.
- 113: `scalar_instance_label`과 `scalar__instance_label`이 함께 있으며 값이 충돌하므로 제외.

103~106의 정답은 원본보다 점이 더 많지만, 원본 점 대부분이 정답과 정확히 대응합니다. 점 순서를 가정하지 않고 XYZ 최근접점 거리가 0.0001 미만인 점만 정답으로 연결했습니다. 매치되지 않는 점은 학습·평가에서 제외했습니다. 실제 추론에는 정답 파일을 읽지 않습니다.

## 모델

XY 간격 4(입력 좌표 단위)의 격자에서 높이·색상·법선·위치·여러 크기의 주변 높이 차이를 추출합니다. ExtraTrees 분류기 2개로 팔레트/부자재와 인접 영역의 경계를 학습하고, 경계 기반 watershed로 개별 물체를 구분합니다. 출력은 원본의 모든 점에 다시 매핑합니다. 딥러닝 모델은 아닙니다.

학습 파일별 최대 14,000개 격자 셀과 샘플링한 인접 경계로 학습했습니다. 격자 정답은 셀 내부 라벨 80% 이상 일치하는 경우만 사용합니다. 기본 분리 기준 0.1, 최소 씨앗 영역 150셀은 검증 파일 124~127의 27개 매개변수 조합 비교로 선택했습니다.

물체가 위아래로 겹쳐 같은 XY 셀에 여러 표면이 있는 경우, 격자 평균으로 인해 경계가 손실될 수 있습니다. 떨어져 보이는 같은 물체를 연결하는 별도의 물체 형상 모델은 없으므로 과분리가 주요 오류입니다.

## 평가 정의

팔레트는 ID 1로 고정합니다. 부자재는 Hungarian 알고리즘으로 정답·예측 간 IoU 합이 최대가 되도록 대응합니다. 부자재 평균 IoU의 분모는 정답 물체 수이며 미대응 정답은 0점입니다. 추가로 만들어진 예측 물체는 F1 계산에서 페널티를 받습니다. 평균은 파일별 지표의 단순 평균입니다. 상세 값은 evaluation.json에 있습니다.

현재 Mac의 추론 및 PLY 저장 예시에서 모델 로드 시간을 제외하면 파일당 약 0.4~0.6초가 측정됐습니다. UI 최초 실행·모델 로드·미리보기 시간은 별도이며, 다른 컴퓨터의 처리시간은 보장하지 않습니다.

## 실행 검증

실제 128번 원본으로 다음을 검사했습니다.

- GUI 파일 추가, 추론 완료, 영역 선택 후 번호 변경.
- 수정 되돌리기, 새 물체 번호 생성.
- 2D 및 3D 미리보기 전환.
- 결과 저장 및 다시 열었을 때 라벨 유지.
- 저장 전후 모든 원본 속성 값의 완전 일치, 점 개수·순서 보존.
- 저장 라벨이 1부터 연속된 정수인지 확인.
- 이미 존재하는 출력 파일 덮어쓰기 방지.

이 검증은 자동 라벨의 완전한 정답성을 의미하지 않습니다. 111번·128번 예시도 수동 검토 전 자동 초안입니다. Windows 실행과 다른 스캐너 조건은 검증하지 않았습니다.
