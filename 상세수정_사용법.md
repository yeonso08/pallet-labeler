# 겹친 부재를 세밀하게 수정하기

이 문서는 해당 기능을 추가할 당시의 기록입니다. 2D 버튼을 포함한 현재 전체 사용법은 [사용법.md](사용법.md)를 보세요.

이번 변경은 화면과 수동 선택 기능만 개선합니다. 자동으로 바닥 점을 판단하거나 라벨을 바꾸지 않으며, 자동 라벨링 모델은 기존 v5 그대로입니다.

## 기본 순서

1. 수정할 부재를 **왼쪽 클릭**합니다. 상단에 `고정: 라벨 번호`가 표시됩니다.
2. **선택 부재만 보기**로 다른 부재를 숨깁니다. 방향 버튼을 눌러도 부재가 고정되어 있으면 해당 부재만 따로 표시합니다.
3. **위 / 앞 / 옆 / 3D**를 선택합니다. 위는 XY, 앞은 XZ, 옆은 YZ, 3D는 회전 가능한 화면입니다. 앞·옆 화면은 `두께 확대`를 끄면 실제 좌표 비율로 볼 수 있습니다.
4. 겹친 점이 있으면 **구간 좁히기**를 켭니다. `높이 Z`, `깊이 Y`, `가로 X` 중 원하는 축을 고르고 시작·끝 슬라이더로 범위를 좁힙니다. 옆의 숫자는 원본 좌표 단위의 실제 범위입니다. 축을 바꾸면 해당 축 전체 범위부터 다시 시작합니다.
5. 표시된 점에 영역을 그려 선택합니다. 고정된 부재의 점만 선택하며, 구간 밖에 숨긴 점은 선택하거나 라벨을 적용할 수 없습니다. 다른 부재와 겹친 위치도 해당 부재로 제한됩니다.
6. 번호를 입력하고 **적용**, 또는 **새 물체로 분리**를 누릅니다. 변경 전 상태는 `되돌리기`로 복원하고, 작업이 끝나면 `수정 결과 저장`을 누릅니다.

슬라이더를 움직이거나 보는 방향을 바꾸면 기존 점 선택을 해제합니다. 숨겨진 구간에 대한 이전 선택이 잘못 적용되지 않도록 하기 위한 동작입니다. 범위를 바꾸는 것은 표시만 바꾸며 원본 점을 삭제하지 않습니다.

## 비교하거나 돌아가기

- **전체 함께 보기**: 현재 방향을 유지하면서 다른 부재도 함께 표시합니다. 부재 고정과 드래그 제한은 유지됩니다. 구간 필터가 켜져 있으면 전체 장면에도 같은 좌표 범위가 적용됩니다.
- **구간 초기화**: 필터를 끄고 전체 구간을 다시 표시합니다.
- **선택 해제 / Esc**: 부재 고정·구간 필터·따로 보기를 해제합니다.
- **팔레트 표시 끄기**: 현재 라벨이 1인 점만 숨깁니다. 다른 부재의 겹침이나 잘못 붙은 부재 라벨은 자동으로 해결하지 않습니다.

고정한 부재가 없는 상태에서 드래그하면 화면에 표시 가능한 전체 영역을 선택합니다. 다른 부재나 팔레트 점을 제외하려면 먼저 원하는 부재를 클릭하세요. 화면 밀도 때문에 생략해 그린 점은 선택 대상에 포함될 수 있지만, 구간 필터나 팔레트 숨김으로 제외한 점은 포함되지 않습니다.

현재 작업 중인 수정 내용을 먼저 저장하고 프로그램을 재시작하면 새 화면이 적용됩니다. 직원 전달본의 별도 v3 모델은 반영하지 않았습니다.

## 확인한 항목

선택·모델 관련 자동 테스트 22개와 실제 Tk 화면 검사를 통과했습니다. XY가 같은 팔레트/다른 부재 제외, XZ/YZ 전환, 높이 범위와 드래그의 일치, Shift 추가 선택의 숨긴 점 제외, 숨겨진 이전 선택의 라벨 적용 방지, 빈 구간, 범위 역전, 초기화와 3D 복귀를 확인했습니다.

재현: `.venv/bin/python -m unittest test_app_selection.py test_segmentation.py test_region_affinity.py`

화면 확인: `.venv/bin/python test_app_view_smoke.py`
