# 수정 완료 7개 반영 및 가림·조각 연결 개선 v5

2026-09-11. 현재 수정 완료된 400~406번 7개로 작업했습니다. 수정 중인 407~409번은 사용하지 않았습니다. 프로그램을 다시 실행하면 기본 선택은 **가림·조각 연결 개선 (v5)**입니다. v4와 이전 모델도 선택할 수 있습니다.

## 최종 변경

v4는 가려진 하나의 부자재가 여러 부분으로 보일 때 각각 별개 물체로 처리하는 경우가 있었습니다. 예를 들어 401번의 정답 물체는 9개인데 자동으로 15개를 만들었습니다.

v5는 v4가 만든 조각에 대해 높이, 평면 방향, 색상, 거리, 크기 등 29개 특성을 비교하는 연결 모델을 추가했습니다. 기존 학습 128개와 수정 완료 7개, 총 135개에서 학습한 조각 쌍은 5,863개이며 같은 물체의 조각 쌍은 223개입니다. 한 조각의 정답 순도가 90% 미만이면 연결 학습에서 제외했습니다. 순도가 낮은 조각까지 정확히 다시 분할하는 기능은 아닙니다.

서로 떨어진 조각도 같은 물체로 연결할 수 있습니다. 그룹을 합칠 때는 그룹 사이 모든 조각 쌍이 기준을 통과해야 합니다. 연결한 영역은 검토 대상으로 표시합니다. 팔레트로 판단한 점은 연결 과정에서 부자재로 바꾸지 않습니다.

**실제 사용하는 v5는 기존 v4 분류기(128개 학습)와 새 조각 연결 모델(135개 학습)의 조합**입니다. 전체 200개를 모두 재학습한 모델은 아닙니다.

## 검증 결과

기존 검증 20개에서 연결 확률 기준 1.0(연결 안 함), 0.98, 0.95, 0.9, 0.8, 0.7, 0.6을 비교했습니다. 팔레트 IoU·부자재 IoU·물체 F1이 v4보다 낮지 않은 후보 중 F1, 부자재 IoU 순으로 선택해 0.6으로 결정했습니다.

| 검증 20개 평균 | v4 | v5 |
|---|---:|---:|
| 부자재 IoU | 88.81% | 89.66% |
| 물체 F1 (IoU 0.5 기준) | 89.85% | 94.45% |
| 팔레트 IoU | 99.51% | 99.51% |

기존 비교용 파일 22개에서도 고정된 설정으로 확인했습니다.

| 파일 | 부자재 IoU v4→v5 | 물체 F1 v4→v5 |
|---|---:|---:|
| 128~133 | 87.80% → 90.01% | 84.23% → 91.61% |
| 166~171 | 90.01% → 92.22% | 87.06% → 94.04% |
| 295~304 | 94.83% → 94.81% | 93.37% → 97.57% |

세 묶음 모두 물체 F1은 개선됐지만 마지막 묶음의 부자재 IoU는 약 0.02%포인트 낮아졌습니다. 기존에 살펴본 파일들에 대한 회귀 확인이며, 독립적인 새 현장 시험 결과는 아닙니다.

## 수정 완료 7개 결과 — 학습 자료임

| 번호 | 정답 물체 수 | v4 물체 수 | v5 물체 수 | 부자재 IoU v4→v5 |
|---|---:|---:|---:|---:|
| 400 | 8 | 10 | 8 | 94.05% → 98.13% |
| 401 | 9 | 15 | 9 | 74.22% → 93.39% |
| 402 | 9 | 9 | 9 | 100.00% → 100.00% |
| 403 | 9 | 10 | 9 | 95.96% → 99.20% |
| 404 | 9 | 9 | 8 | 81.95% → 84.63% |
| 405 | 9 | 10 | 9 | 93.81% → 95.46% |
| 406 | 9 | 14 | 10 | 87.92% → 97.76% |

7개 평균 부자재 IoU는 89.70%→95.51%, 물체 F1은 86.21%→98.41%입니다. 이 7개는 연결 모델 학습에 사용했으므로 새 파일에 대한 성능으로 해석하면 안 됩니다. 404번은 합쳐짐, 406번은 추가 조각 문제가 여전히 있습니다. 물체 수가 맞아도 모든 경계가 맞는 것은 아닙니다.

## 시도했으나 기본 모델에 적용하지 않은 실험

- 작은 시작 영역 제거 기준 확대 및 경계 중앙값 필터: 과분할이 줄기도 하지만 검증 영역 정확도가 내려가 선택하지 않았습니다.
- 기존 128개+수정 7개로 원래 분류기를 재학습: 검증 부자재 IoU 88.81%→88.66%, 물체 F1 89.85%→89.55%로 하락했습니다. 해당 후보는 `results/corrected7_v5`에 보관하며 프로그램에서 사용하는 v5와 다릅니다.
- 최종 선택은 `results/region_link_v5/model_v5.joblib`입니다. 후처리 실험 폴더 `segmentation_v5`, `segmentation_v5_smoothing`의 후보도 기본 모델에 연결하지 않았습니다.

## 사용·파일·검사

프로그램 재시작 후 원본을 열거나 이미 열린 파일에 **선택 파일 다시 자동 라벨링**을 실행하면 v5가 적용됩니다. 이미 저장한 수정본을 열기만 하면 기존 라벨을 유지합니다. CLI는 `--model results/region_link_v5/model_v5.joblib`를 지정합니다.

- 최종 모델: `results/region_link_v5/model_v5.joblib`
- 연결 학습 통계: `results/region_link_v5/training.json`
- 검증 및 비교: 같은 폴더의 `validation.json`, `regression.json`
- 자동 예시: `results/region_link_v5/example/401_labeled.ply` (수동 검수 정답 아님)
- 학습 재현: `.venv/bin/python train_region_linker.py`
- 원래 분류기 재학습 실험 재현: `.venv/bin/python retrain_corrected7.py`
- 시작 영역 크기 실험: `.venv/bin/python improve_oversegmentation.py --mode size`
- 경계 잡음 완화 실험: `.venv/bin/python improve_oversegmentation.py --mode smoothing`
- 자동 테스트: `.venv/bin/python -m unittest test_segmentation.py test_region_affinity.py`

401번을 CLI에서 실제 처리·저장·재읽기해 원본의 모든 점과 필드를 보존하고, 저장된 결과가 평가 기록과 일치함을 확인했습니다. 분리 로직, 링크 없는 기존 모델 호환, 연쇄 합침 방지, 한 점짜리 조각의 유효한 특성 계산, 연결 모델 호출 등을 자동 테스트합니다.

원본·자동 결과·수정본 폴더의 파일은 변경하지 않았습니다. 남은 407~409번은 향후 수정 완료 시 별도 확인에 사용할 수 있지만 연속 촬영의 유사성 때문에 완전히 독립적인 장면이라는 보장은 없습니다.
