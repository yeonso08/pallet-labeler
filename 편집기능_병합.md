# 직원 전달본의 편집 기능 병합

후속 개선으로 단일 옆에서 보기 버튼을 위·앞·옆·3D 및 구간 필터로 확장했습니다. 현재 사용법은 `상세수정_사용법.md`를 참조하세요. 아래는 최초 병합 당시 기록입니다.

`/Users/hwangjaeyeon/Desktop/PalletLabeler1`에서 옆에서 보기(XZ)와 선택한 부재 안에서 드래그 선택하는 기능을 현재 프로그램에 반영했습니다. 전달본의 모델 파일, 모델 설정, 학습 코드와 자동 라벨링 엔진은 가져오지 않았습니다. 현재 기본 모델은 가림·조각 연결 개선 v5입니다.

## 사용 방법

1. 부재를 **왼쪽 클릭**하면 해당 라벨 전체가 선택되고 드래그 범위가 그 라벨 안으로 고정됩니다. 전체 선택은 원본 색을 비쳐 표시합니다.
2. 마우스로 영역을 그리면 고정한 라벨의 점만 선택됩니다. 같은 화면 위치에 겹친 팔레트나 다른 라벨의 부재는 포함되지 않습니다. Shift/Alt로 영역을 추가·제거해도 이 범위를 넘지 않습니다.
3. **옆에서 보기**를 누르면 그 라벨만 XZ 평면에 표시됩니다. 높이를 따라 영역을 그려 일부 점만 선택할 수 있습니다. 세로는 보기 편하게 늘려 표시하므로 실제 두께 비율과 다릅니다.
4. **위에서 보기**는 고정을 유지한 채 XY 화면으로 돌아갑니다. **선택 해제 / Esc**는 고정을 풀고 전체 화면으로 돌아갑니다.

고른 부재가 없는 상태의 드래그는 기존처럼 전체 영역 선택입니다. 따라서 다른 라벨의 점을 제외하려면 먼저 부재를 클릭하세요. 이미 부재 라벨로 잘못 붙어 있는 바닥 점은 같은 라벨에 속하므로 함께 선택될 수 있으며, 옆에서 보기에서 높이로 잘라 수정할 수 있습니다.

전달본은 2D 오른쪽 클릭으로 부재를 골랐지만 현재 프로그램은 왼쪽 클릭을 사용하므로 현재 조작을 유지했습니다. 오른쪽 드래그 회전/이동, 휠 드래그 이동, 3D 점 선택, 화면 이동 시 성능 최적화, 저장 대화상자와 원본 보호 기능도 유지했습니다.

부분 수정 뒤에는 기존 부재가 남아 있으면 고정을 유지합니다. 라벨이 전부 다른 번호로 바뀌어 사라지면 고정과 옆에서 보기를 해제합니다. 저장 시 라벨 번호가 정리되면 고정 번호도 함께 갱신합니다.

## 확인

- 선택·저장·되돌리기 및 기존 모델 관련 자동 테스트 18개 통과.
- 실제 Tk 화면에서 v5 기본 선택, 선택 부재만 XZ 표시, 높이 영역 선택, 선택 해제, 3D 복귀 확인.
- 확인 이미지: `results/ui_merge_check/side_view.png`.
- 일반 테스트: `.venv/bin/python -m unittest test_app_selection.py test_segmentation.py test_region_affinity.py`
- 실제 화면 확인: `.venv/bin/python test_app_view_smoke.py` (테스트 창만 생성 후 닫음).

열려 있는 수정 내용은 먼저 저장한 뒤 프로그램을 다시 실행하면 새 기능이 적용됩니다.
