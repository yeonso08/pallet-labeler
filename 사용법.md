# Pallet Labeler 설명서

팔레트 스캔 PLY의 **자동 라벨링 초안을 만들고 사람이 수정해서 저장하는 프로그램**입니다.
팔레트는 라벨 1, 부자재는 2부터 물체마다 다른 번호로 저장합니다.

이 문서 하나로 설치·화면·조작·저장·한계를 모두 다룹니다.
버전별 변경 내역은 [CHANGELOG.md](CHANGELOG.md), 모델별 검증 수치는 문서 끝의 [기록 문서](#기록-문서)를 보세요.

---

## 목차

1. [실행하기](#1-실행하기)
2. [화면 구성](#2-화면-구성)
3. [기본 작업 순서](#3-기본-작업-순서)
4. [자동 라벨링 모델 고르기](#4-자동-라벨링-모델-고르기)
5. [마우스와 키보드](#5-마우스와-키보드)
6. [보는 방법 바꾸기](#6-보는-방법-바꾸기)
7. [겹친 부재를 세밀하게 수정하기](#7-겹친-부재를-세밀하게-수정하기)
8. [라벨 고치기](#8-라벨-고치기)
9. [저장하기](#9-저장하기)
10. [여러 파일 한 번에 처리하기](#10-여러-파일-한-번에-처리하기)
11. [명령줄로 실행하기](#11-명령줄로-실행하기)
12. [성능과 적용 범위](#12-성능과-적용-범위)
13. [문제가 생기면](#13-문제가-생기면)
14. [파일 구성](#14-파일-구성)

---

## 1. 실행하기

**현재 Mac**: 이 폴더의 `시작.command`를 Finder에서 더블클릭합니다. 실행 환경은 설치되어 있습니다.

**다른 컴퓨터**: Python 3.13과 Tkinter가 필요합니다.

- Mac: 압축을 풀고 `시작.command` 실행. 처음 한 번은 패키지를 내려받습니다.
- Windows: Python 3.13 설치 후 `start_windows.bat` 실행. Windows 실제 실행은 아직 검증하지 않았습니다.
- 현재 Mac의 `.venv` 폴더는 다른 컴퓨터로 복사하지 마세요. 배포 ZIP에도 넣지 않습니다.
- 패키지 설치에만 인터넷이 필요합니다. **PLY 처리와 모델 추론은 전부 이 컴퓨터에서 실행하며 파일을 외부로 보내지 않습니다.**

모델 파일과 `requirements.txt`의 라이브러리 버전을 함께 유지하세요.

프로그램이 이미 떠 있는 상태에서 새 버전을 적용하려면, 수정 내용을 먼저 저장하고 창을 닫은 뒤 다시 실행합니다.

---

## 2. 화면 구성

창 전체가 점군 화면과 같은 어두운 색입니다. 강조색은 스캔 속 팔레트 목재의 주황색이며, **수정 결과 저장** 버튼과 **켜져 있는 표시 버튼**에만 씁니다. 버튼 글자가 주황색이면 켜진 상태입니다.

### 왼쪽 패널 — 파일과 자동 라벨링

| 항목 | 하는 일 |
|---|---|
| 자동 라벨링 모델 | 사용할 모델을 고릅니다. 고르면 해당 모델의 분리 기준 기본값이 함께 들어갑니다 |
| PLY 파일 추가 / 폴더에서 추가 | 작업할 파일을 목록에 넣습니다 |
| 파일 목록 | 더블클릭하면 바로 엽니다. 여러 개를 함께 고를 수 있습니다 |
| 선택 파일 열기 / 자동 라벨링 | 고른 파일을 열고 라벨을 만듭니다 |
| 선택 파일 목록에서 빼기 | 목록에서만 뺍니다. 디스크의 파일은 그대로입니다 |
| 물체 경계 분리 기준 | 왼쪽일수록 잘게 나누고 오른쪽일수록 크게 묶습니다 |
| 선택 파일 다시 자동 라벨링 | 저장된 라벨을 무시하고 현재 모델·기준으로 다시 계산합니다 |
| 저장 폴더 | 저장 창과 일괄 처리가 처음 가리킬 폴더입니다. 기본값은 프로그램 폴더의 `results` |
| 추가한 파일 일괄 처리 | 목록의 모든 파일을 자동 초안으로 저장합니다 |

### 오른쪽 위 — 수정 도구줄

`선택한 점 → 라벨` 입력칸, **적용**, **새 물체로 분리**, **되돌리기**, 그리고 오른쪽 끝에 **수정 결과 저장**이 있습니다.

### 오른쪽 둘째 줄 — 표시 설정

색상 방식, 표시 밀도, **팔레트 표시**, **번호 표시**, 점 크기, **선택 해제**, **화면 맞춤**.

### 오른쪽 셋째 줄 — 보는 방향

**선택 부재만 보기**, **2D · 위 · 앞 · 옆 · 3D** 버튼, **두께 확대**, 그리고 지금 고정된 부재 번호 표시.

### 오른쪽 넷째 줄 — 구간 좁히기

**구간 좁히기** 켜기, 축 선택(높이 Z / 깊이 Y / 가로 X), 시작·끝 슬라이더, 실제 좌표 범위 표시, **구간 초기화**.

### 캔버스와 상태줄

가운데가 점군 화면입니다. 왼쪽 위 모서리에 파일 이름, 표시 중인 점 수 / 전체 점 수, 고정 라벨, 보는 방향이 적힙니다. 창 맨 아래 한 줄은 마지막 동작의 결과를 알려 줍니다.

---

## 3. 기본 작업 순서

1. **PLY 파일 추가** 또는 **폴더에서 추가**로 파일을 넣습니다.
2. 목록에서 파일을 고르고 **선택 파일 열기 / 자동 라벨링**을 누릅니다. 더블클릭도 같습니다.
3. 화면에서 잘못 나뉜 곳을 찾습니다. 「물체별 색상」으로 보면 파란색이 팔레트(1), 나머지 색이 부자재입니다.
4. 부재를 클릭해 고정하고, 보는 방향과 구간을 조절해 가며 점을 골라 라벨을 고칩니다.
5. **수정 결과 저장**을 누르고 이름과 위치를 정합니다.
6. 저장한 PLY를 CloudCompare에서 열어 `instance_label`로 확인합니다.

이미 `scalar_instance_label`이 들어 있는 PLY를 열면 **기존 라벨을 그대로 불러옵니다.** 자동 라벨링을 다시 돌리지 않습니다. 새로 계산하려면 **선택 파일 다시 자동 라벨링**을 누르세요.

원본 디스크 파일은 어떤 경우에도 변경하지 않습니다.

---

## 4. 자동 라벨링 모델 고르기

왼쪽 맨 위에서 고릅니다. 목록 맨 위가 기본값입니다.

| 모델 | 분리 기준 기본값 | 설명 |
|---|---:|---|
| 가림·조각 연결 개선 (v5) | 0.03 | **현재 기본값.** v4 결과에서 같은 물체의 흩어진 조각을 다시 이어 붙입니다 |
| 팔레트 혼입 개선 (v4) | 0.03 | 부자재에 둘러싸인 팔레트 점이 부자재로 바뀌던 문제를 고친 버전 |
| 추가 학습 모델 (v2) | 0.06 | 이전 기본 모델 |
| 새 데이터 학습 모델 (v3) | 0.03 | 새 데이터로 학습했지만 기존 장면에서 하락이 있어 기본으로 쓰지 않습니다 |
| 이전 모델 (v1) | 0.1 | 최초 모델 |

모델을 바꾸면 **선택 파일 다시 자동 라벨링**을 눌러야 실제로 적용됩니다. 이미 화면에 열려 있는 라벨은 바뀌지 않습니다.

**분리 기준 슬라이더**는 한 덩어리를 몇 개의 물체로 볼지 정합니다. 하나의 부재가 여러 개로 쪼개지면 오른쪽으로, 여러 부재가 하나로 붙으면 왼쪽으로 옮기고 다시 자동 라벨링하세요.

---

## 5. 마우스와 키보드

조작은 2D와 3D가 같습니다.

| 조작 | 동작 |
|---|---|
| 왼쪽 드래그 | 그린 영역 안의 점을 선택합니다. 선택한 점은 흰색이 됩니다 |
| 왼쪽 클릭 | 클릭한 부재 전체를 선택하고 그 부재로 **고정**합니다 |
| 같은 부재 다시 클릭 / 빈 곳 클릭 | 선택과 고정을 해제합니다 |
| Shift + 왼쪽 드래그 | 지금 선택에 더합니다 |
| Alt + 왼쪽 드래그 | 지금 선택에서 뺍니다 |
| 오른쪽 드래그 | 3D에서는 회전, 2D에서는 화면 이동 |
| 휠 누른 채 드래그 | 화면 이동 |
| 휠 굴리기 | 확대·축소 |
| Esc | 선택·고정·구간·따로 보기를 모두 해제합니다 |
| 목록에서 Delete / Backspace | 고른 파일을 목록에서 뺍니다 |

**부재 고정이 핵심입니다.** 부재를 한 번 클릭해 고정하면, 그 뒤의 드래그 선택은 **그 라벨의 점만** 잡습니다. 화면상 같은 자리에 겹친 팔레트나 다른 부재는 들어오지 않습니다. Shift·Alt로 영역을 더하고 빼도 이 범위를 벗어나지 않습니다.

고정하지 않은 상태의 드래그는 화면에 보이는 전체 영역을 선택합니다. 다른 부재를 섞지 않으려면 **먼저 클릭해서 고정하세요.**

**선택은 화면에 보이는 대로가 아니라 깊이 방향으로 관통합니다.** 2D에서는 같은 XY 자리에 있는 여러 높이의 점이 함께 선택되고, 3D에서도 뒤에 가려진 점이 함께 들어옵니다. 이때 쓰는 것이 아래의 방향 전환과 구간 좁히기입니다.

화면에는 속도를 위해 점의 일부만 그리지만, **영역 선택과 저장은 전체 점에 적용합니다.**

---

## 6. 보는 방법 바꾸기

### 방향 버튼

| 버튼 | 보이는 평면 |
|---|---|
| **2D** | 3D에서 빠져나와 **마지막에 보던 평면**으로 돌아갑니다 |
| **위** | 위에서 내려다본 XY |
| **앞** | 앞에서 본 XZ. 높이를 기준으로 자를 때 씁니다 |
| **옆** | 옆에서 본 YZ |
| **3D** | 회전할 수 있는 입체 화면 |

지금 보고 있는 방향의 버튼에 불이 들어옵니다. 2D 화면에서는 **2D**와 해당 평면 버튼 둘 다 켜집니다.

**부재를 고정한 상태에서 방향 버튼을 누르면 그 부재만 따로 보여 줍니다.** 다른 부재에 가려지지 않은 상태로 형태를 확인하기 위한 동작입니다.

**두께 확대**는 앞·옆 화면에서만 의미가 있습니다. 켜면 얇은 부재를 보기 좋게 세로로 늘려 그립니다. 실제 비율로 보려면 끄세요.

### 표시 설정

- **원본 색상**: 스캔 당시의 RGB. 기본값입니다. RGB가 없는 파일은 물체별 색상으로 그립니다.
- **물체별 색상**: 팔레트는 파란색, 부자재마다 서로 다른 색.
- **높이 색상**: 낮은 곳에서 높은 곳까지 높이에 따른 색.
- **빠르게 / 촘촘하게 / 전체 점**: 화면에 그릴 점 수입니다. 각각 6만, 20만, 전부이며 기본은 촘촘하게입니다. **저장에는 언제나 전체 점을 씁니다.**
- **팔레트 표시**: 끄면 라벨 1의 점만 숨깁니다. 숨긴 팔레트는 선택과 수정에서도 빠집니다.
- **번호 표시 / 점 크기**: 물체 번호 표시 여부와 점의 굵기.
- **화면 맞춤**: 확대·회전을 초기 시점으로 되돌립니다. 라벨은 건드리지 않습니다.
- **선택 해제**: Esc와 같습니다.

화면을 회전하거나 확대하는 동안에는 점이 굵고 성기게 보이다가, 손을 멈추면 원래 밀도로 다시 그려집니다. 부드럽게 움직이기 위한 표시 방식이며 점이나 라벨은 바뀌지 않습니다.

색상·밀도·점 크기·번호 표시는 **화면 표시만** 바꿉니다. PLY의 RGB나 라벨에는 영향이 없습니다.

---

## 7. 겹친 부재를 세밀하게 수정하기

부재가 서로 겹쳐 있거나 바닥 점이 부재에 붙어 있을 때의 순서입니다.

1. 고칠 부재를 **왼쪽 클릭**합니다. 도구줄에 `고정: 라벨 번호`가 나타납니다.
2. **선택 부재만 보기**를 눌러 다른 부재를 숨깁니다. 다시 누르면 **전체 함께 보기**로 돌아가며, 이때도 고정과 드래그 제한은 유지됩니다.
3. **앞** 또는 **옆**을 눌러 높이가 보이는 화면으로 바꿉니다.
4. 아직 겹쳐 보이면 **구간 좁히기**를 켭니다. 축을 고르고 시작·끝 슬라이더로 범위를 줄이세요. 옆의 숫자는 원본 좌표 단위의 실제 범위입니다.
5. 남은 점에 영역을 그려 선택하고 라벨을 고칩니다.
6. **구간 초기화**로 필터를 끄고, **Esc**로 고정까지 풀면 전체 화면으로 돌아옵니다.

구간 좁히기에 대해 알아 둘 점:

- **먼저 부재를 고정해야 켜집니다.** 슬라이더 범위는 고정한 부재의 해당 축 전체 범위를 0~100%로 나눈 것입니다.
- 축을 바꾸면 그 축의 전체 범위부터 다시 시작합니다.
- **숨긴 구간의 점은 선택할 수도, 라벨을 바꿀 수도 없습니다.** Shift로 더해도 들어오지 않습니다.
- 슬라이더를 움직이거나 방향을 바꾸면 기존 점 선택이 풀립니다. 보이지 않는 점에 실수로 라벨이 적용되는 것을 막기 위한 동작입니다.
- 범위를 좁히는 것은 **표시와 선택 범위만** 바꿉니다. 점을 지우지 않습니다.

이 기능들은 화면과 수동 선택만 바꿉니다. 바닥 점을 자동으로 판단하거나 라벨을 자동으로 고치지 않습니다.

---

## 8. 라벨 고치기

- **라벨 번호 입력 → 적용**: 선택한 점을 그 번호로 바꿉니다. 팔레트로 되돌리려면 `1`을 넣습니다. 1 이상의 정수만 받습니다.
- **합치기**: 한쪽 부재를 고른 다음, 합칠 상대 부재의 번호를 입력하고 적용합니다.
- **새 물체로 분리**: 선택한 점을 지금까지 없던 새 번호로 떼어 냅니다.
- **되돌리기**: 최근 수정 20회까지 한 단계씩 되돌립니다.

라벨을 바꾼 점은 `scalar_review_needed`가 0으로 바뀝니다. 사람이 확인한 점으로 보기 때문입니다.

부분만 수정해서 원래 부재가 남아 있으면 고정은 유지됩니다. 그 라벨이 전부 다른 번호로 바뀌어 사라지면 고정과 방향이 자동으로 풀립니다.

---

## 9. 저장하기

**수정 결과 저장**을 누르면 저장 창이 열립니다. 이름과 위치를 직접 정합니다. 기본 이름은 `원래이름_labeled.ply`입니다.

- 이미 있는 파일을 고르면 덮어쓸지 먼저 물어봅니다.
- **불러온 원본 PLY는 덮어쓸 수 없습니다.** 목록에 있는 파일도 마찬가지입니다.
- 저장할 때 부자재 번호의 빈칸을 정리해 2, 3, 4…로 다시 매깁니다. 팔레트는 1로 유지됩니다. 고정해 둔 번호도 함께 따라갑니다.
- 원본의 점 개수·순서·XYZ·RGB·법선·기존 속성을 그대로 보존합니다.

저장되는 필드:

| 필드 | 의미 |
|---|---|
| `scalar_instance_label` | 팔레트 1, 부자재 2부터. CloudCompare 저장 형식과 같습니다 |
| `scalar_review_needed` | 1은 경계이거나 판별이 애매한 점, 0은 나머지 |

`scalar_review_needed`는 정답 확률이 아니고 오류가 없다는 보장도 아닙니다. **0인 영역도 확인이 필요합니다.**

---

## 10. 여러 파일 한 번에 처리하기

**추가한 파일 일괄 처리**를 누르면 목록의 모든 파일을 현재 모델·기준으로 처리해 저장 폴더에 넣습니다.

- 출력 이름은 `원래이름_labeled.ply`이고, 같은 이름이 있으면 숫자를 붙여 새 파일로 저장합니다. 덮어쓰지 않습니다.
- 파일별 결과와 실패 이유는 같은 폴더의 `batch_*.json`에 남습니다.
- **일괄 결과는 자동 초안입니다.** 사람이 검수한 정답이 아닙니다. CloudCompare나 이 프로그램에서 확인한 뒤 학습에 쓰세요.

처리 중에는 파일 목록을 바꾸거나 다른 작업을 시작할 수 없습니다.

---

## 11. 명령줄로 실행하기

```sh
.venv/bin/python label.py /입력/폴더 --output /결과/폴더 \
  --model results/region_link_v5/model_v5.joblib
```

- `--model`을 생략하면 `model.joblib`(v2)을 씁니다. 화면과 같은 v5를 쓰려면 위처럼 지정하세요.
- `--split`으로 분리 기준을 직접 줄 수 있습니다. 0과 1 사이여야 합니다.
- 같은 이름의 결과가 이미 있으면 덮어쓰지 않고 그 파일을 실패로 기록합니다.
- 실행 기록은 결과 폴더의 `run_*.json`에 남습니다.

---

## 12. 성능과 적용 범위

기본 모델 v5의 검증 20개 파일 평균입니다.

| 측정 항목 | v4 | v5 |
|---|---:|---:|
| 팔레트 IoU | 99.51% | 99.51% |
| 부자재 평균 IoU | 88.81% | 89.66% |
| 물체 F1 (IoU 0.5 기준) | 89.85% | 94.45% |

IoU는 예측 영역과 정답 영역의 교집합을 합집합으로 나눈 값입니다. **89.66%가 모든 점의 정답률이나 완성된 파일의 비율을 뜻하지는 않습니다.** 부자재 번호 자체는 비교하지 않고, 영역이 가장 잘 겹치는 물체끼리 대응시켜 계산했습니다.

알아 두어야 할 한계:

- 하나의 부재를 둘 이상으로 나누거나 서로 다른 부재를 합치는 오류가 남아 있습니다. v5에서 줄었지만 없어지지는 않았습니다.
- 위 수치는 이미 여러 번 살펴본 파일들에 대한 회귀 확인입니다. **한 번도 보지 않은 새 현장 데이터의 성능 증명이 아닙니다.**
- 원본과 정답의 좌표가 정확히 일치하는 점만 평가했습니다.
- **제공된 스캐너의 위치·좌표계·단위와 비슷한 촬영 조건을 전제로 합니다.** Z축이 높이인 데이터로 학습했습니다. 회전된 점군, 다른 단위, 새로운 팔레트나 배경에 대한 성능은 검증하지 않았습니다.
- 이 프로그램의 3D 화면은 CloudCompare와 같은 렌더링 엔진이 아닙니다. 최종 확인은 CloudCompare에서 하세요.

`examples_v2/166_labeled.ply`, `examples_v2/128_labeled.ply`는 v2 자동 처리 예시이고, `examples` 폴더에는 이전 모델의 111번·128번 예시가 있습니다. `results/region_link_v5/example/401_labeled.ply`는 v5 예시입니다. **모두 자동 결과이며 사람이 수정한 정답이 아닙니다.**

---

## 13. 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| 모델을 바꿨는데 화면이 그대로 | **선택 파일 다시 자동 라벨링**을 눌러야 적용됩니다 |
| 파일을 열었는데 자동 라벨링이 돌지 않음 | 이미 `scalar_instance_label`이 있는 파일입니다. 기존 라벨을 불러온 것입니다 |
| 구간 좁히기가 반응하지 않음 | 먼저 부재를 클릭해서 고정하세요 |
| 드래그하면 다른 부재까지 잡힘 | 부재를 클릭해 고정한 뒤 드래그하세요 |
| 선택한 점에 적용이 안 됨 | 구간이나 팔레트 숨김으로 가려진 점은 수정되지 않습니다. 구간을 넓히세요 |
| 화면이 비어 있음 | 구간을 너무 좁혔거나 따로 보기 상태입니다. **구간 초기화** 또는 Esc |
| 앞·옆 화면의 비율이 이상함 | **두께 확대**가 켜져 있습니다. 끄면 실제 비율입니다 |
| 저장이 거부됨 | 불러온 원본 PLY에는 덮어쓸 수 없습니다. 다른 이름을 쓰세요 |
| 처리 중이라고만 나옴 | 자동 라벨링이나 일괄 처리가 끝날 때까지 다른 작업은 잠깁니다 |
| 화면이 느림 | 표시 밀도를 **빠르게**로 낮추세요. 선택과 저장은 그대로 전체 점에 적용됩니다 |

수정 중인 내용이 있는 상태에서 다른 파일을 열거나 목록에서 빼면 먼저 물어봅니다. 저장하지 않고 진행하면 그 수정은 사라집니다.

---

## 14. 파일 구성

| 파일 | 내용 |
|---|---|
| `app.py` | 실행 화면, 표시, 선택·수정·저장 |
| `theme.py` | 화면 색상과 글꼴 |
| `viewer.py` | 점 색상 계산과 표시용 점 추출 |
| `engine.py` | PLY 입출력, 자동 라벨링, 영역 분리 |
| `region_affinity.py` | 흩어진 조각을 같은 물체로 잇는 v5 연결 모델 |
| `label.py` | 명령줄 일괄 처리 |
| `model_config.json` | 화면에 보이는 모델 목록과 기본 분리 기준 |
| `train.py`, `retrain.py`, `train_v3.py`, `train_region_linker.py` | 학습 재현 스크립트 |
| `improve_segmentation.py`, `improve_oversegmentation.py` | 후처리 개선 실험 재현 |
| `results/` | 모델 파일, 검증 기록, 예시 결과 |

자동 테스트:

```sh
.venv/bin/python -m unittest test_app_selection.py test_segmentation.py test_region_affinity.py
```

실제 Tk 화면 확인 (테스트 창을 만들고 바로 닫습니다):

```sh
.venv/bin/python test_app_view_smoke.py
```

### 기록 문서

| 문서 | 내용 |
|---|---|
| [CHANGELOG.md](CHANGELOG.md) | 버전별 변경 내역 |
| [조각연결_v5_결과.md](조각연결_v5_결과.md) | v5 연결 모델 학습과 검증 |
| [분리개선_v4_결과.md](분리개선_v4_결과.md) | v4 팔레트 혼입 개선 |
| [학습결과_v3.md](학습결과_v3.md) | v3 데이터 점검과 학습 |
| [성능하락_원인분석.md](성능하락_원인분석.md) | v3에서 성능이 내려간 원인 |
| [새파일_400_406_평가.md](새파일_400_406_평가.md) | 수동 수정본 7개 평가 |
| [상세수정_사용법.md](상세수정_사용법.md) | 세밀 수정 기능 추가 당시 기록 |
| [편집기능_병합.md](편집기능_병합.md) | 직원 전달본 편집 기능 병합 기록 |
| [검증기록.md](검증기록.md) | v1 학습 방식과 검증 범위 |
| [추가학습_결과.md](추가학습_결과.md) | v2 추가 학습 결과 |
