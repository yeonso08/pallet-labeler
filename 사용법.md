# 화면 안내

화면 배치는 이전과 같고 색상만 바뀌었습니다. 창 전체가 점군 화면과 같은 어두운 색이며, 강조색은 스캔 속 팔레트 목재의 주황색입니다. **팔레트 표시**와 **번호 표시**는 켜져 있을 때 글자가 주황색이 되고, **수정 결과 저장**은 항상 주황색입니다.

기본은 스캔 파일에 저장된 **원본 RGB 색상**입니다. 어두운 배경에서 3D로 회전하거나 마우스 휠로 확대할 수 있습니다. 물체를 합치거나 영역을 나눌 때는 **2D / 3D** 버튼으로 2D에 들어가세요.

- **원본 색상**: 스캔 당시 색상과 질감. RGB가 없는 파일은 물체별 색상으로 표시합니다.
- **물체별 색상**: 팔레트는 파란색, 각 부자재는 서로 다른 선명한 색.
- **높이 색상**: 낮은 곳부터 높은 곳까지 높이에 따른 색상.
- **빠르게 / 촘촘하게 / 전체 점**: 표시할 점 수. 기본 20만 점입니다. 저장에는 언제나 전체 점을 사용합니다.
- **팔레트 표시**: 끄면 부자재만 보입니다. 숨긴 팔레트는 2D 영역 선택에서도 제외됩니다.
- **점 크기 / 번호 표시**: 보기 편한 크기와 번호 표시 여부를 선택합니다.
- **선택 해제 / 화면 맞춤**: 선택을 풀거나 처음 시점으로 돌아갑니다.

회전하거나 확대하는 동안에는 점이 굵고 성기게 보이다가, 손을 멈추면 곧바로 원래 밀도로 다시 그려집니다. 움직임을 부드럽게 하기 위한 표시 방식이며 실제 점이나 라벨은 바뀌지 않습니다.

선택한 점은 **흰색**으로 표시됩니다. 원래 색을 보려면 선택 해제를 누르세요. 물체별 색상, RGB, 높이 색상은 화면 표시만 바꾸며 PLY의 RGB나 라벨은 바꾸지 않습니다. 3D 렌더링은 CloudCompare와 동일한 엔진이 아니며, 작업용 2D 선택 기능은 그대로 유지합니다.

기존에 열려 있는 창에서 수정 중이었다면 먼저 저장한 뒤 닫고 시작.command를 다시 실행하세요. 모델과 자동 라벨링 결과는 v2와 동일합니다.

---

# Pallet Labeler v2 — PLY 자동 라벨링

추가 데이터 재학습 버전입니다. 화면 왼쪽에서 v2 / v1 모델을 선택할 수 있습니다. 상세 비교는 [추가학습 결과](추가학습_결과.md)를 확인하세요. 이미 실행 중이면 수정 내용을 저장하고 프로그램을 다시 실행하세요.

제공된 LVS 스캐너 PLY로 학습한 **자동 초안 생성 + 수동 수정 프로그램**입니다. 팔레트는 1, 부자재는 2부터 각각 다른 번호로 저장합니다. 자동 결과를 검토한 뒤 AI 학습에 사용하세요. 겹친 물체의 분리·합치기는 아직 사람이 수정해야 하는 경우가 있습니다.

## 현재 Mac에서 실행

이 폴더의 **시작.command**를 Finder에서 더블클릭하세요. 현재 Mac에는 실행에 필요한 환경을 설치해 두었습니다.

1. **PLY 파일 추가** 또는 **폴더에서 추가**로 라벨링할 파일을 선택합니다.
2. 목록에서 파일을 선택하고 **선택 파일 열기 / 자동 라벨링**을 누릅니다.
3. 원본 색상이 기본입니다. 「물체별 색상」을 선택하면 파란색은 팔레트(1), 나머지 색상은 부자재입니다.
4. 잘못 나뉜 부분을 수정하고 **수정 결과 저장**을 누릅니다.
5. 저장된 PLY를 CloudCompare에서 열고 `instance_label`을 확인합니다.

이미 `scalar_instance_label`이 있는 PLY를 열면 기존 라벨을 불러옵니다. **선택 파일 다시 자동 라벨링**은 선택 파일을 새로 추론합니다. 원본 디스크 파일은 변경하지 않습니다.

## 빠르게 수정하기

- **2D에서 마우스로 영역을 둘러 그리기:** 영역 안의 점을 선택합니다. 선택한 점은 흰색으로 표시됩니다.
- **2D에서 오른쪽 클릭:** 해당 번호의 물체 전체를 선택합니다.
- **라벨 번호 입력 → 적용:** 선택한 점을 지정한 번호로 바꿉니다. 팔레트로 바꾸려면 1을 입력합니다.
- **합치기:** 한 물체를 오른쪽 클릭한 다음, 합칠 대상 물체의 번호를 입력하고 적용합니다.
- **새 물체로 분리:** 선택한 점을 새 번호로 분리합니다.
- **되돌리기:** 최근 수정 20회까지 되돌립니다.
- **2D / 3D:** 3D에서는 회전하며 형태를 확인할 수 있습니다. 점 선택과 수정은 2D에서 합니다.

2D 선택은 위에서 내려다본 XY 영역의 모든 점에 적용됩니다. 같은 XY 위치에 여러 높이의 점이 겹친 경우도 함께 선택되므로, 이런 세밀한 수정은 CloudCompare에서 확인하세요. 화면에는 속도를 위해 일부 점만 표시하지만, 영역 선택과 저장은 전체 점에 적용합니다.

분리 기준 슬라이더를 왼쪽으로 움직이면 더 잘게, 오른쪽으로 움직이면 더 크게 묶는 경향이 있습니다. 값을 바꾼 후 **선택 파일 다시 자동 라벨링**을 눌러 적용합니다. v2 기본값은 검증 데이터로 선택한 0.06이며, v1은 0.1입니다. 모델을 선택하면 해당 기본값이 적용됩니다.

## 일괄 처리와 저장 형식

**추가한 파일 일괄 처리**를 누르면 선택한 저장 폴더에 자동 초안이 저장됩니다. 각 파일 결과와 실패 이유는 같은 폴더의 `batch_*.json`에서 확인할 수 있습니다.

- 원본의 점 개수·순서·XYZ·RGB·법선·기존 속성을 보존합니다.
- 라벨 속성의 PLY 이름은 `scalar_instance_label`입니다. 제공된 CloudCompare 저장 형식과 같습니다.
- 추가 필드 `scalar_review_needed`: 1은 경계 또는 판별이 애매한 점, 0은 나머지 점입니다. 정답 확률이나 오류 보장은 아닙니다. 0인 영역도 검토가 필요합니다.
- 출력 이름은 `원래이름_labeled.ply`입니다. 같은 이름이 있으면 GUI는 숫자를 붙여 새 파일로 저장합니다.
- 수동 저장할 때 부자재 번호의 빈칸을 정리해 2, 3, 4…로 만듭니다. 팔레트는 1입니다.
- 기본 저장 폴더는 프로그램 폴더 안의 `results`입니다. 화면에서 다른 폴더로 바꿀 수 있습니다.

`examples_v2/166_labeled.ply`와 `examples_v2/128_labeled.ply`는 v2 실제 자동 처리 예시입니다. `examples` 폴더에는 이전 모델의 111번·128번 예시를 보존했습니다. **사람이 수정한 정답이 아닙니다.** 111번은 정답 라벨이 없어 정확도를 측정할 수 없습니다.

## 확인한 성능과 적용 범위

학습에 넣지 않은 128~133번 6개 파일에서 파일별 수치를 평균한 v2 결과입니다. 추가 파일 166~171의 부자재 평균 IoU는 89.5%였으며, 같은 파일의 v1 결과는 88.5%였습니다.

| 측정 항목 | 결과 |
|---|---:|
| 팔레트 영역 IoU | 97.1% |
| 개별 부자재 평균 IoU | 87.0% |
| 물체 검출 F1, IoU 0.5 기준 | 84.1% |

IoU는 예측 영역과 정답 영역의 교집합을 합집합으로 나눈 값입니다. **87.0%가 모든 점의 정답률이나 완성된 파일의 비율을 뜻하지는 않습니다.** 부자재 번호 자체는 비교하지 않고, 영역이 가장 잘 겹치는 물체끼리 대응시켜 평가했습니다.

겹쳐져 가려진 하나의 물체를 둘 이상으로 나누는 오류가 남아 있습니다. 팔레트 가장자리나 낮은 부자재도 확인이 필요합니다. 원본과 정답의 좌표가 정확히 일치하는 점만 평가했고, 삭제되거나 일치하지 않는 점은 점수에서 제외했습니다.

**현재 제공된 스캐너의 위치·좌표계·단위와 유사한 촬영 조건을 전제로 합니다.** Z축이 높이인 데이터로 학습했습니다. 회전된 점군, 다른 단위, 새로운 팔레트·배경·물체에 대한 성능은 검증하지 않았습니다. 같은 촬영 환경의 파일 6개 평가이므로 전체 현장 성능을 보장하는 수치는 아닙니다.

## 다른 컴퓨터로 옮길 때

Python 3.13과 Tkinter를 사용할 수 있는 환경이 필요합니다. 현재 Mac의 `.venv`는 다른 컴퓨터로 복사하지 마세요. 배포 ZIP에는 `.venv`를 포함하지 않았습니다.

- Mac: 압축 해제 후 `시작.command` 실행. 처음에는 필요한 패키지를 설치합니다.
- Windows: Python 3.13 설치 후 `start_windows.bat` 실행. Windows 실제 실행 검증은 아직 하지 않았습니다.
- 처음 패키지를 설치할 때 인터넷이 필요합니다. **PLY 처리·모델 추론은 로컬에서 실행되며 파일을 외부에 전송하지 않습니다.**

모델 파일과 라이브러리 버전을 함께 유지하세요. `requirements.txt`는 이번 검증에 사용한 버전입니다.

## 파일 구성

- `app.py`: 실행 화면, 미리보기, 수정·저장
- `theme.py`: 화면 색상과 글꼴 등 표시 설정
- `engine.py`: 점군 입출력 및 자동 라벨링
- `model.joblib`: 제공 데이터로 학습한 모델
- `label.py`: 명령줄 일괄 처리
- `train.py`: 이번 데이터 번호 체계를 사용하는 학습·평가 재현 스크립트
- `evaluation_v2.json`: v2와 v1의 파일별 비교 및 데이터 분할
- `evaluation.json`: 첫 버전의 검증 기록
- `검증기록.md`: 학습 방식과 검증 범위

명령줄로 실행하는 경우:

```sh
.venv/bin/python label.py /입력/폴더 --output /결과/폴더
```

명령줄 처리는 같은 이름의 결과가 있으면 덮어쓰지 않고 해당 파일을 실패로 기록합니다.
