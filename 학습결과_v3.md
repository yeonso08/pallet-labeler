# before / after 데이터 학습 결과

2026-09-11에 두 폴더의 PLY 파일을 점검하고 새 모델을 실제 학습·평가했습니다.
프로그램을 다시 실행하면 모델 선택에서 **새 데이터 학습 모델 (v3)**를 사용할 수 있습니다.
기존 평가 장면에서 성능 하락이 있어 기본 선택은 v2를 유지했습니다.

## 데이터

- 원본 202개, 완료본 200개. 동일 파일명으로 대응했습니다.
- 사용 가능한 170개: 학습 128개, 설정 선택용 검증 20개, 평가 22개.
- 이전 학습 명세의 학습·검증·평가 구분을 유지했습니다. 새 파일 중 마지막 10개(295~304)는 평가, 앞선 10개(285~294)는 검증으로 확보했습니다.
- 사용 파일의 좌표 대응률 최솟값은 98.2925%입니다. 대응되지 않은 점은 학습 및 점수 계산에서 제외합니다.
- 사용한 원본의 XYZ 배열 SHA-256 중복은 없었습니다. 같은 장면의 다른 촬영이나 점 순서가 다른 중복까지 배제한 것은 아닙니다.
- 완료본 누락: 226, 269.
- 명시적인 instance_label 없음: 134, 172~199, 총 29개. Original_cloud_index를 임의로 정답으로 사용하지 않았습니다.
- 라벨 필드 중복: 113. 두 필드 중 어느 것이 정답인지 불명확하여 제외했습니다.
- 원본과 완료본 파일은 수정하지 않았습니다. 사용자가 제공한 완료본을 정답으로 간주했으며 수작업으로 모든 라벨의 의미를 검수하지는 않았습니다.

## 같은 정답으로 비교한 결과

| 평가 파일 | 지표 | v2 | v3 |
|---|---|---:|---:|
| 295~304, 10개 | 부자재 평균 IoU | 94.01% | 94.30% |
| 295~304, 10개 | 물체 F1 (IoU ≥ 0.5) | 92.79% | 93.37% |
| 295~304, 10개 | 팔레트 IoU | 99.70% | 99.63% |
| 128~133, 6개 | 부자재 평균 IoU | 86.67% | 85.83% |
| 128~133, 6개 | 물체 F1 (IoU ≥ 0.5) | 84.84% | 83.08% |
| 166~171, 6개 | 부자재 평균 IoU | 89.49% | 88.35% |
| 166~171, 6개 | 물체 F1 (IoU ≥ 0.5) | 87.04% | 85.47% |

새 장면에서는 부자재 IoU가 0.29%포인트 개선됐지만, 기존 두 평가 묶음에서는 각각 0.84, 1.13%포인트 하락했습니다. 전반적으로 더 좋은 모델이라고 결론 내릴 수 없습니다. 기존 보고서와 정답 파일이 달라질 수 있으므로 이번 표는 두 모델 모두 현재 after 파일로 다시 평가한 결과입니다.

기존과 같은 ExtraTrees 분류기 구조로 팔레트/부자재 및 경계를 재학습했습니다. 검증 20개에서 분리 기준 0.03, 0.06, 0.1, 0.15, 0.2를 비교해 부자재 IoU 평균이 가장 높은 0.03을 선택했습니다. 평가 파일은 모델 학습과 설정 선택에 사용하지 않았습니다. 연속 촬영 파일 사이의 상관관계가 있을 수 있으며, 이 점수는 다른 현장에 대한 성능 보장이 아닙니다.

## 파일과 재현

- `results/training_v3/model_v3.joblib`: 학습 완료 모델.
- `results/training_v3/manifest.json`: 파일별 사용 구분, 대응률, 제외 사유.
- `results/training_v3/validation.json`: 검증과 설정 선택 결과.
- `results/training_v3/evaluation.json`: 새 평가 10개 비교.
- `results/training_v3/retained_evaluation.json`: 기존 평가 12개 비교.
- `train_v3.py`: 점검, 학습, 검증 및 새 데이터 평가.
- `evaluate_retained_v3.py`: 기존 평가 파일 비교.

프로젝트 폴더에서 `.venv/bin/python train_v3.py` 실행 후 `.venv/bin/python evaluate_retained_v3.py`를 실행하면 재현할 수 있습니다. 같은 v3 결과 경로를 갱신합니다. `model.joblib`의 기존 v2 모델은 보존됩니다.
