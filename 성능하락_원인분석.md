# v3 성능 하락 원인 분석 — 2026-09-11

기존 평가 12개와 새 평가 10개에서 두 모델에 동일한 분리 설정을 적용했습니다. 또한 팔레트/부자재 분류 결과와 경계 예측 결과를 v2·v3 사이에서 교차 조합해 영향을 분리했습니다. 모델과 프로그램 설정은 변경하지 않았습니다.

## 분리 설정의 영향

기존 평가 12개에서 파일별 부자재 IoU를 평균했습니다.

| 모델 | 분리 설정 | 부자재 IoU | 물체 F1 |
|---|---:|---:|---:|
| v2 | 0.06 | 88.0784% | 85.9436% |
| v2 | 0.03 | 87.7679% | 86.0063% |
| v3 | 0.03 | 87.0917% | 84.2769% |
| v3 | 0.06 | 87.7521% | 84.0732% |

v3에서 설정만 0.03→0.06으로 바꾸면 IoU가 0.6605%포인트 회복됩니다. 원래 v2와 v3 차이 0.9867%포인트의 약 67%입니다. 이는 이 평가 묶음에서 선택한 비교 경로에 따른 수치이며, 모든 성능 하락의 원인을 일반적으로 67% 설명한다는 뜻은 아닙니다. F1은 개선되지 않아 설정만 되돌리는 것이 완전한 해결책은 아닙니다.

## 학습된 모델의 영향

0.06으로 통일한 기존 평가 12개 결과:

| 팔레트/부자재 분류 | 경계 예측 | 부자재 IoU |
|---|---|---:|
| v2 | v2 | 88.0784% |
| v3 | v2 | 88.0609% |
| v2 | v3 | 87.7691% |
| v3 | v3 | 87.7521% |

분류 모델만 바꾸면 변화는 -0.0175%포인트로 작습니다. 경계 모델만 바꾸면 -0.3093%포인트입니다. 같은 설정에서 남는 하락은 주로 경계 예측과 그에 따른 영역 분리에 연결됩니다. 왜 학습된 경계가 바뀌었는지, 데이터 구성·라벨 일관성·표본 추출 중 무엇이 원인인지는 이번 실험만으로 확정하지 않았습니다.

## 실제 하락 파일에서 확인한 오류

정답의 물체 번호별로 예측 영역에 포함된 점을 직접 집계했습니다. 아래 팔레트 점 수는 해당 물체의 주된 예측 영역에 섞인 정답 라벨 1의 점 수입니다.

| 파일 / 정답 물체 | v2 (0.06) | v3 (0.03) | v3 (0.06) |
|---|---:|---:|---:|
| 171 / 물체 7: 섞인 팔레트 점 | 58 | 35,146 | 68 |
| 131 / 물체 6: 섞인 팔레트 점 | 129 | 24,889 | 24,889 |
| 129 / 물체 8: 섞인 팔레트 점 | 72 | 5,593 | 5,593 |

- 171.ply: 파일 부자재 IoU 86.94%→79.34%. 설정 0.06에서는 86.87%로 회복. 물체 7의 IoU는 약 96.4%→28.5%→96.4%입니다.
- 131.ply: 파일 부자재 IoU 79.50%→75.48%. 설정을 맞춰도 동일합니다. 물체 6의 IoU가 약 81.4%→25.9%로 하락했습니다.
- 129.ply: 파일 부자재 IoU 86.29%→84.83%. 설정을 맞춰도 동일합니다. 물체 8에 팔레트 영역이 붙었습니다.

핵심은 팔레트로 정답 처리된 점들 중 부자재로 잘못 검출된 영역이, 경계 변화 때문에 실제 부자재와 하나의 인스턴스로 합쳐지는 현상입니다. 전체 팔레트/부자재 분류 점수는 거의 같아도, 잘못 검출된 점들이 어느 물체에 붙느냐에 따라 물체 IoU는 크게 떨어집니다. 단순 과분할·부자재끼리 합쳐짐 개수만으로는 이 오류를 충분히 설명하지 못합니다.

## 분리 설정 설명의 정정

`engine.py`는 경계 확률이 설정값보다 낮은 영역을 물체 분리의 시작 영역으로 삼고, 150셀보다 작은 시작 영역은 제거합니다. 시작 영역이 하나도 없으면 대체 시작점을 만듭니다. 따라서 값을 낮춰도 물체가 항상 더 잘게 나뉘지는 않습니다. 시작 영역이 사라져 오히려 넓은 영역이 합쳐질 수 있습니다. 이전의 '낮으면 더 잘게 나누는 쪽'이라는 설명은 이 동작을 충분히 반영하지 못했습니다.

## 다음 개선 작업

현재 기본 모델 v2를 유지하는 것이 타당합니다. 다음 구현 실험은 잘못 검출된 팔레트 영역이 부자재 인스턴스로 퍼지는 현상과 작은 시작 영역 제거 후 합쳐지는 현상을 대상으로 해야 합니다. 정답과 비교한 실패 사례를 바탕으로 검증 데이터에서도 같은 오류가 있는지 확인하고, 검증 데이터에서 개선안을 선택해야 합니다.

이번 평가 파일들은 원인 분석에 이미 사용했으므로, 이 파일들을 겨냥해 고친 뒤 같은 파일의 점수만으로 일반적인 성능 개선을 주장할 수 없습니다. 최종 성능 확인에는 별도 장면을 확보하는 것이 필요합니다. 이번 작업에서는 재학습·알고리즘 수정·설정 변경을 하지 않았습니다.

재현: `.venv/bin/python diagnose_v3.py`

전체 176개 조합 결과와 과분할/합쳐짐 집계 기준은 `results/training_v3/diagnosis.json`에 있습니다. 과분할/합쳐짐 집계는 10% 이상 겹침 및 50점 이상 기준의 진단용 지표입니다.
